import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Firebase requires real initialization — integration tests cover E2E flows.
    expect(true, isTrue);
  });
}
